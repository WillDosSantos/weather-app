import React from "react";

const Titles = () => (
  <div>
    <h1 className="logo">Simple Weather</h1>
    <p className="tagline">The World's best Weather App</p>
  </div>
);

export default Titles;
